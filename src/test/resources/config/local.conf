app {
  host = "http://localhost:5601"
  version = "8.0.0"
}

es {
  scheme = "https"
  host = "localhost"
  port = 9200
}

security {
  on = true
}

auth {
  providerType = "basic"
  providerName = "basic"
  username = "elastic"
  password = "changeme"
}